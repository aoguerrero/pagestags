<!DOCTYPE html>
<html lang="en">
<head>
<title>$page_title</title>
</head>
<body>
<xmp theme="simplex" style="display:none;">

#set ( $d = "$")
#foreach ($selectedTag in $selectedTags)<a class="btn btn-primary" href="/pages/list/${TemplateUtil.removeTagFromList($selectedTagsStr, $selectedTag)}">$selectedTag</a> #end
#foreach ($tag in $tags)<a class="btn btn-secondary" href="/pages/list/${selectedTagsStr}#if (!$selectedTags.isEmpty())$d#end${tag}">$tag</a> #end


#foreach ($item in $items)
- <a href="/pages/$item.id/view">$item.title</a>  
#foreach ($tag in $item.tags)*$tag* #end#if(!$item.pblic)private#end


#end
#if (!$filtered)
Select a category from the top.
#end

<a href="/">Main Page</a>#if ($auth)<a href="/pages/new"> | New Page</a> | <a href="/logout">Logout</a> #end

</xmp>
</body>
<script src="https://cdn.jsdelivr.net/gh/arturadib/strapdown@gh-pages/v/0.2/strapdown.min.js"></script>
</html>
